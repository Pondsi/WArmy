{
  "name": "warmy",
  "version": "0.1.0",
  "private": true,
  "description": "Workhorse Army (WArmy / 无限牛马) — local-first multi-agent group-chat desktop app",
  "author": "Pondsi",
  "license": "MIT",
  "engines": {
    "node": ">=24.0.0"
  },
  "packageManager": "pnpm@10.23.0",
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "verify": "node spikes/verify-all/run.mjs"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "better-sqlite3",
      "electron",
      "esbuild"
    ]
  },
  "devDependencies": {
    "typescript": "5.9.3"
  }
}
