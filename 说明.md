# 说明（WArmy / 无限牛马）

> 完整中文说明书。英文总览与多语言摘要见 [README.md](./README.md)。

## 1. 这是什么

**WArmy**（全名 **Workhorse Army**，中文名 **无限牛马**）是一款**本地优先的多智能体群聊桌面应用**。

副标题（中文）：**让AI成为你的无限牛马**。  
非中/日/韩界面副标题：**An infinite army of AI workhorses working for you.**

你可以：

1. 创建多个 AI 实例（**牛马**），每个实例有独立人格、模型、工作区；
2. 把它们编入**项目群**，由本机**值班者**编排工具与任务；
3. 在看板上看进展，在回退点上回档；
4. 可选：让项目**只在容器里开发**（安全隔离）；
5. 可选：多台机器组成**本机组网**，成员可见项目状态。

核心承诺：**注入模型的上下文有固定上限，但不丢失历史细节**（有界渲染 + 指针可回取）。

---

## 2. 优点（为什么选它）

### 2.1 产品形态上的优点

| 优点 | 说明 |
| --- | --- |
| **群聊是主界面，不是附属** | 项目/联系人/外部群/我的牛马是一等公民，不是“聊天框 + 插件” |
| **值班编排，而不是抢麦** | 本机序号值班、定向 @、纯调度值班者、优先级队列，规则写死可预期 |
| **队列不丢** | P2/P3 可编辑；Router 弹出必须处理，失败回队；进程退出队列落盘 |
| **容器语义诚实** | 未就绪不静默降级；没真 commit 不报“已固化”；WSL 无 commit 就禁用 |
| **文件改动是产品功能** | 工具访问台账、项目目录是**项目级、成员可见**，不是本机暗账 |
| **本地优先** | 默认不上传对话/文件；身份、组网、看板都在本机数据目录 |
| **端口不说谎** | 默认 59599，失败必报错 + 可点选候选，绝不静默换端口 |
| **十种语言** | 键集对齐、运行时可达，不是“文件在但选不到” |

### 2.2 工程上的优点

- Electron + TypeScript，**主/渲染进程零原生模块**（原生能力隔离在 Worker / 子进程）
- pnpm monorepo，职责拆成 `group-router` / `sync-protocol` / `board` / `memory-os` 等包
- 大量**可复跑验证脚本**（i18n、接线、协议、容器探针、Router、e2e、UI 巡检）
- ADR 文档化决策（上下文渲染、组网身份、容器环境）

---

## 3. 和同类产品的不同 / 优势

> 下面是**定位差异**，不是贬低任何项目。很多场景应组合使用。

### 3.1 与「智能体提示词包 / 角色库」

例如 agency-agents 这类仓库：提供大量 **Agent 人格与工作流模板**，非常适合作为灵感与提示词来源。

**WArmy 的不同**：交付的是**可安装运行的桌面产品**——实例生命周期、群值班、持久化、身份、容器项目策略、发布端口与安装包，而不只是 Markdown 角色文件。

### 3.2 与「单窗口聊天 UI」

许多开源聊天壳擅长：多供应商、对话记录、简单换模型。

**WArmy 的不同**：工作单元是 **项目群 + 值班 + 看板 + 工具门禁 + 容器策略**。你要的是“一支队伍怎么干活”，不是“一个框怎么聊”。

### 3.3 与「编程智能体 CLI / IDE 插件」

例如 Claude Code、Cursor、Aider、Gemini CLI 等：在**你的编辑器/终端/仓库**里做深度代码修改，体验往往极好。

**WArmy 的不同**：**不是 IDE 插件**。它是**项目指挥台**：多智能体聊天、成员可见状态、容器开发策略、组网身份。项目目录可以指向真实仓库，但产品重心是**协作编排与隔离策略**，不是替换 IDE。

### 3.4 与「多智能体框架（库）」

例如 AutoGen / CrewAI / LangGraph 一类：代码优先，灵活度高。

**WArmy 的不同**：框架要你自己搭宿主应用、UI、权限、持久化。WArmy 是**开箱桌面应用**（界面、语言包、队列落盘、安全门禁、安装包）。

### 3.5 与「桌面智能体运行时 / Agent OS」

例如 OpenClaw 类桌面运行时、dsh 风格智能体执行器：本机执行与工具生态往往更强。

**WArmy 的关系**：可以**互补**。WArmy 把“智能体能跑”推进到“**队伍怎么编、项目怎么停、容器什么时候算可用、成员看到什么**”这套**产品语义**，并公开端口与协议常量（`WARMY-*`）。

### 3.6 与「容器 / 远程开发平台」

Docker Desktop、云 IDE 等容器体验专业。

**WArmy 的不同**：

- **不捆绑** Docker Desktop（许可友好，避免把用户拖进商业订阅争议）；
- 探测**用户已安装**运行时（Docker/Podman/WSL/…）；
- 把容器定义成**开发环境（安全隔离）**，原则上**不用于项目测试**；
- 控制台**只能是容器内 shell**，未就绪置灰。

### 3.7 一句话对比

| 你如果想要… | 更合适的形态 |
| --- | --- |
| 一组可复制的 Agent 提示词 | 角色库 / prompt pack |
| 一个好看的聊天框 | 单窗口聊天 UI |
| 在 IDE 里改代码 | 编程智能体 CLI/插件 |
| 用代码搭多智能体流水线 | 框架库 |
| **一支有编制、有纪律、可隔离开发的 AI 队伍桌面产品** | **WArmy** |

---

## 4. 安装方法

### 4.1 Windows 安装包（推荐）

1. 打开 [Releases v0.1.0](https://github.com/Pondsi/WArmy/releases/tag/v0.1.0)
2. 下载 **`WArmy-Setup-0.1.0.exe`**  
   直链：[WArmy-Setup-0.1.0.exe](https://github.com/Pondsi/WArmy/releases/download/v0.1.0/WArmy-Setup-0.1.0.exe)
3. 可选：用 [`checksums.txt`](./checksums.txt) 校验 SHA-256
4. 运行安装包，启动 **WArmy / 无限牛马**
5. **设置**里配置语言、主题、模型供应商

### 4.2 源码构建

```bash
git clone https://github.com/Pondsi/WArmy.git
cd WArmy
pnpm install
pnpm -r build
pnpm --filter @warmy/app-shell start
```

需要：**Node.js ≥ 24**、**pnpm**（仓库锁定 `pnpm@10.23.0`）。

打包安装包（可选）：

```bash
pnpm --filter @warmy/app-shell dist
```

---

## 5. 使用方法

### 5.1 界面地图

| 区域 | 用途 |
| --- | --- |
| 我的牛马 | 管理本机 AI 实例，一对一对话 |
| 项目 | 项目群：值班编排、容器策略、文件/产物面板 |
| 联系人 / 群聊 | 外部协作；外部群仅 @ 响应 |
| 总看板 | 只读聚合进展（由值班者写入） |
| 设置 | 语言、主题、供应商、容器探测、组网、身份、端口 |

### 5.2 新手路径

1. **我的牛马** → 创建牛马 → 配置供应商与模型 → 启动  
2. **项目** → 创建项目 → 选择开发环境：  
   - **本机**：常规开发  
   - **容器中开发**：安全隔离；每次启动项目必须先启动容器；宿主侧编辑会被拒绝  
3. 把牛马拉进项目 → 发送消息，选择紧急度：  
   - **P1 加急**：尽快插入  
   - **P2 插入**（默认）：当前任务完成后插入  
   - **P3 排队**：进队列，可编辑/删除  
4. 右侧查看：最近改动文件、其他文件、生成的产品、进度、指标  
5. **设置 → 语言**：可在 10 种语言间切换（界面立即生效）

### 5.3 值班编排怎么跑

```text
用户发言
  → Router（外部群无 @ 则静默）
  → 值班者（仅本机）生成状态卡片
  → 执行者短命任务 + 工具
  → 回写看板 / 知识库
  → 精简回复回聊天
  → 冲刷队列（弹出必处理，失败回队，不丢弃）
```

### 5.4 容器开发项目规则（必读）

- 容器定位是**开发环境（安全隔离）**，**原则上不用于项目测试**。
- 容器未就绪 ⇒ 控制台置灰，**绝不静默改在主机执行**。
- 项目目录可能 bind mount 进容器（宿主可见是设计的一部分）；应用仍**拒绝宿主侧编辑**。
- 「固化当前环境」按运行时能力表执行；**没有真 commit + 回读镜像，就绝不写“已固化”**。
- 环境回退 ≠ 文件回退：回退点管文件；镜像/固化管环境。

### 5.5 组网端口

| 常量 | 默认值 |
| --- | --- |
| 生产 | **59599** |
| 开发约定 | 58588 |
| 测试约定 | 62666 |

绑定失败会明确报错，并在设置里给出**可点选**候选端口；**绝不静默换端口**。

---

## 6. 技术要点

| 领域 | 做法 |
| --- | --- |
| 壳 | Electron 33 + TypeScript |
| 原生模块 | 主/渲染零原生模块；sqlite/嵌入等隔离到 Worker/子进程 |
| 仓库 | pnpm monorepo 多包 |
| 上下文 | 有界渲染器 + 指针 retrieve，上限固定、细节不丢 |
| 路由 | 值班状态机 + 优先级队列 + 磁盘序列化 |
| 身份组网 | Ed25519 指纹、成员证书、`WARMY-*` 协议常量 |
| 容器 | 探测三态 + 能力表 + 证据等级 |
| i18n | 单一语言列表源 + 键集必须相等 |
| 验证 | 可复跑脚本 + CDP UI 巡检 |

架构长文：[`references/ARCHITECTURE.md`](./references/ARCHITECTURE.md)  
决策记录：[`docs/ADR/`](./docs/ADR/)

---

## 7. 文件操作范围

- 本地数据默认在应用用户数据目录（**默认不上传**）
- 项目目录由创建者选择；容器项目通过挂载进容器
- 工具文件访问台账是**项目功能**（成员可见），不是本机隐藏日志
- Windows 可选目录 ACL 锁（可撤销）；不是加密

---

## 8. 限制与风险（务必阅读）

- **容器未启动 = 项目不可用**（成员面等同创建者下线）；**历史仍可读**
- 应用拒绝宿主侧编辑，但**无法阻止**你用外部编辑器打开 bind mount 目录
- ACL 锁不是加密/沙箱，管理员或系统进程仍可能写入
- Docker Desktop 对较大组织的商业使用可能有付费订阅；本产品**不把它设为必需依赖**
- 跨公网可达性、NAT 打洞等需要真实第二台机器；当前验证以本机/同网为主
- 移动端是**外观预览**；编排在桌面端完成

---

## 9. 法律

按 [LICENSE](./LICENSE) 提供，**现状**、无担保。作者 **Pondsi** 完全免责；衍生作品须保留 Pondsi 署名。  
AI 模型输出由其提供方负责，不代表本软件立场。

赞助见 [SPONSORS.md](./SPONSORS.md)。问题请用仓库 Issue 模板提交。

---

Pondsi（+mimo-X-por-Preview +mimo-v2.5-pro +DeepSeek-V4.1-Flash +Qwen3.7-max +Qwen3.8-27b +Gemini3.1pro +Gemini3.8-flash）— 由 Xiaomi MiMo Desktop 自行提交
