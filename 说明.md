# 说明（WArmy / 无限牛马）

## 这是什么

WArmy（全名 **Workhorse Army**，中文名 **无限牛马**）是一款多智能体群聊桌面应用。
副标题：让AI成为你的无限牛马。

你可以创建多个 AI 实例（牛马），把它们编入项目群聊，由「值班者」负责编排工具调用、看板任务与上下文预算。

## 怎么装

### Windows 安装包

1. 到本仓库 Releases 下载 `无限牛马 Setup 0.1.0.exe`
2. 运行安装，按向导完成
3. 打开应用 → 设置，配置模型供应商与 API Key（若使用云端模型）

### 源码构建

```bash
pnpm install
pnpm -r build
pnpm --filter @warmy/app-shell start
```

需要 Node.js ≥ 24 与 pnpm。

## 怎么用（概要）

1. **我的牛马**：管理本机 AI 实例，可单独聊天
2. **项目**：创建项目群（可选择「是否在容器中开发」）
3. **联系人 / 群聊**：协作与外部群（外部群仅 @ 才响应）
4. **总看板**：聚合各会话进展（由值班者写入）
5. **设置**：语言（10 种）、主题、容器探测、组网、身份等

## 文件操作范围

- 本地数据默认保存在应用用户数据目录（不上传）
- 项目目录由创建者选择；容器项目通过 bind mount 挂载进容器
- 工具文件访问台账是**项目级**功能（成员可见），不是本机隐藏日志

## 限制与风险

- **容器未启动 = 项目不可用**（等同创建者下线）；历史记录仍可读
- 应用侧拒绝宿主侧编辑，但**无法阻止**用户用外部编辑器打开 bind mount 目录
- Windows 上可选「锁定项目目录」（icacls deny-write），可一键撤销；不是加密/沙箱
- Docker Desktop 对较大组织的商业使用可能有付费订阅；产品不把它设为必需依赖
- 默认端口：生产 59599、开发 58588、测试 62666；绑定失败会明确报错，不会静默换端口
- 组网跨公网可达性、NAT 打洞等需要真实第二台机器，当前套件只在本机/同网验证

## 法律

按 LICENSE 提供，**现状**、无担保。作者 Pondsi 完全免责；使用风险自担。
AI 模型输出由其提供方负责，不代表本软件立场。

---

Pondsi（+mimo-X-por-Preview +mimo-v2.5-pro +DeepSeek-V4.1-Flash +Qwen3.7-max +Qwen3.8-27b +Gemini3.1pro +Gemini3.8-flash）— 由 Xiaomi MiMo Desktop 自行提交
