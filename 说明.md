# 说明（WArmy / 无限牛马）

> **完整中文说明书**。英文主入口见 [README.md](./README.md)（本文件是中文权威说明）。

## 1. 这是什么

**WArmy**（全名 **Workhorse Army**，中文名 **无限牛马**）是**本地优先的多智能体群聊桌面应用**。

- 中文副标题：**让AI成为你的无限牛马**
- 非中/日/韩副标题：**An infinite army of AI workhorses working for you.**

你可以：

1. 创建多个 AI 实例（**牛马**），独立人格 / 模型 / 工作区；
2. 编入**项目群**，由本机**值班者**编排工具与任务；
3. 使用**总看板 / 回退点 / 知识库**；
4. 可选**容器中开发**（安全隔离）；
5. 可选**本机组网**，成员可见项目状态；
6. 使用产品级**记忆系统**（JSONL + FTS + 向量，recall/retrieve）。

核心承诺：**注入模型的上下文有固定上限，但不丢失历史细节**（有界渲染 + 指针 + 记忆服务可回取）。

---

## 2. 优点（为什么选它）

### 2.1 产品形态

| 优点 | 说明 |
| --- | --- |
| **群聊是主界面** | 项目 / 联系人 / 外部群 / 我的牛马是一等公民 |
| **值班编排** | 本机值班、定向 @、纯调度、P0–P3 队列落盘不丢 |
| **记忆系统是特色** | JSONL 唯一事实 + 可重建投影 + 三路召回 + 工具解引用 |
| **容器语义诚实** | 未就绪不静默降级；无真 commit 不报已固化 |
| **文件台账是产品功能** | 项目级、成员可见 |
| **本地优先** | 默认不上传对话/文件 |
| **端口不说谎** | 默认 59599，失败必报错 + 可点选候选 |
| **十种语言** | 键集对齐、运行时可达 |

### 2.2 工程

- Electron + TypeScript，主/渲染**零原生模块**（sqlite 在子进程）
- pnpm monorepo：`memory-os` / `group-router` / `sync-protocol` / `board` 等
- 可复跑验证：i18n、接线、协议、容器、Router、**记忆**、文档、UI 巡检
- ADR 文档化（上下文渲染、组网身份、容器环境）

---

## 3. 记忆系统（产品重点）

> 这是 WArmy 的**核心能力之一**，不是附属缓存。

### 3.1 架构

```text
聊天 / 值班 / 工具
        │ append
        ▼
fast-memory.jsonl  ──唯一事实来源（只追加、写入者唯一）
        │ rebuild / project
        ▼
memory.db（SQLite）
  · records 投影（可丢弃、可全量重建）
  · fts_uni（CJK 单字）
  · fts_tri（trigram：标识符/路径/错误码）
  · vec_index（可选向量，bge int8）
        │ RRF 融合
        ▼
recall → 索引卡  /  retrieve → 逐字节原文
```

### 3.2 给模型用的工具

| 工具 | 作用 |
| --- | --- |
| `recall(query)` | 语义/关键词发现证据卡（多通道 + RRF） |
| `retrieve(seq=…)` / `retrieve(recordId=…)` | 按锚点取回**完整原文**（有界截断时可续） |

上下文渲染器会把被裁掉的历史收成**指针**（seq 范围 / recordId），模型可解引用，而不是“忘了就没了”。

### 3.3 运行时与安全

- 记忆服务是**长驻子进程**（`@warmy/memory-os`）；`better-sqlite3` **不进** Electron 主/渲染进程。
- 数据目录：应用用户数据下的 `memory/`；可用环境变量 **`WARMY_MEMORY_DIR`** 覆盖（兼容旧名 `CCA_ARMY_MEMORY_DIR`）。
- 服务未就绪时工具**如实失败**，不编造历史。
- 会话日志在启动时从 JSONL **重建**（不变量：日志是唯一事实来源之一）。
- 回退点会把 `fast-memory.jsonl` 纳入文件层记录（与容器环境固化分层）。

### 3.4 验证

```bash
node packages/app-shell/scripts/verify-memory.mjs
node packages/app-shell/scripts/verify-context-renderer.mjs
```

历史 spike 压测：`spikes/p2-memory/`（FTS/向量/召回语料）。

---

## 4. 和同类产品的不同

（与英文 README 同一立场：定位差异，可组合使用）

| 类型 | 常见长处 | WArmy 差异 |
| --- | --- | --- |
| Agent 提示词/角色库 | 人格模板丰富 | **可安装桌面产品** + 记忆/值班/容器策略 |
| 单窗口聊天 UI | 换模型聊天简单 | 项目群 + 值班 + 看板 + **记忆** + 门禁 |
| 编程 CLI/IDE 插件 | 仓库内改代码 | **项目指挥台**，不是 IDE 插件 |
| 多智能体框架库 | 代码灵活 | 开箱 UI + 持久化 + 安全门禁 |
| 桌面智能体运行时 | 本机执行强 | 队伍编制 + 项目停用面 + 记忆产品化 |
| 容器平台 | 容器体验专业 | 不捆绑 Docker Desktop；探测用户运行时 |

一句话：给你**一支有编制、有记忆、可隔离开发的 AI 队伍桌面产品**。

---

## 5. 安装方法

### 5.1 Windows 安装包

1. [Releases v0.1.0](https://github.com/Pondsi/WArmy/releases/tag/v0.1.0)
2. 下载 **`WArmy-Setup-0.1.0.exe`** — [直链](https://github.com/Pondsi/WArmy/releases/download/v0.1.0/WArmy-Setup-0.1.0.exe)
3. 可选对照 [`checksums.txt`](./checksums.txt)
4. 安装后启动 **WArmy / 无限牛马** → 设置里配置语言与模型供应商

### 5.2 源码构建

```bash
git clone https://github.com/Pondsi/WArmy.git
cd WArmy
pnpm install
pnpm -r build
pnpm --filter @warmy/app-shell start
```

需要 **Node.js ≥ 24**、**pnpm**。

可选拉取随包 Node 运行时（打包/记忆子进程）：

```bash
node scripts/fetch-node-runtime.mjs
```

> 仓库默认**不**把 `resources/node` 的大体积二进制强行进 git（避免 >50MB 警告）；用上述脚本按平台下载。

贡献流程见 [CONTRIBUTING.md](./CONTRIBUTING.md)。

---

## 6. 使用方法

### 6.1 界面地图

| 区域 | 用途 |
| --- | --- |
| 我的牛马 | 实例管理与一对一对话 |
| 项目 | 值班编排、容器策略、文件/产物、记忆检索 |
| 联系人 / 群聊 | 外部协作；外部群仅 @ 响应 |
| 总看板 | 只读聚合进展 |
| 设置 | 语言、主题、供应商、容器、组网、身份、端口、技能 |

### 6.2 新手路径

1. 我的牛马 → 创建 → 配置供应商/模型 → 启动  
2. 项目 → 创建 → 选择**本机**或**容器中开发**  
3. 发消息选紧急度：P1 加急 / P2 插入 / P3 排队  
4. 右侧看文件与进展；知识库可检索/入库  
5. 设置 → 语言（10 种，立即生效）

### 6.3 值班编排

```text
用户发言 → Router（外部群无 @ 静默）
  → 值班者（仅本机）状态卡片
  → 执行者 + 工具（含 recall/retrieve）
  → 看板/知识库回写
  → 精简回复
  → 队列冲刷（弹出必处理，失败回队）
```

### 6.4 容器开发规则

- 容器 = **开发环境（安全隔离）**，原则上不用于项目测试  
- 未就绪 ⇒ 控制台置灰，**不静默主机执行**  
- 容器未启动 = 项目不可用（成员等同创建者下线），**历史可读**  
- 固化需真 commit + 镜像回读；WSL 无 commit 则禁用并如实说明  
- 环境回退 ≠ 文件回退  

### 6.5 端口

| 用途 | 默认 |
| --- | --- |
| 生产 mesh TCP | **59599** |
| 开发约定 | 58588 |
| 测试约定 | 62666 |

绑定失败**绝不静默换端口**。

---

## 7. 技术要点

| 领域 | 做法 |
| --- | --- |
| 壳 | Electron 33 + TypeScript |
| 记忆 | JSONL + SQLite FTS/向量 + recall/retrieve |
| 上下文 | 有界渲染 + 指针解引用 |
| 路由 | 值班状态机 + 队列落盘 |
| 身份组网 | Ed25519 + `WARMY-*` 协议 |
| 容器 | 探测三态 + 能力表 + 证据等级 |
| i18n | 10 语言键集对齐 |
| 验证 | verify-* 脚本 + CDP UI 巡检 |

架构：[`references/ARCHITECTURE.md`](./references/ARCHITECTURE.md) · ADR：[`docs/ADR/`](./docs/ADR/)

---

## 8. 文件操作范围

- 本地数据在应用用户数据目录（**默认不上传**）
- 记忆 JSONL / sqlite 同在用户数据 `memory/`
- 项目目录由创建者选择；台账项目级可见
- Windows 可选目录 ACL 锁（可撤销）

---

## 9. 限制与风险

- 容器未启动 = 项目不可用；历史可读  
- 应用拒绝宿主侧编辑，无法阻止外部编辑器打开 bind mount 目录  
- ACL 锁 ≠ 加密/沙箱  
- Docker Desktop 不作为必需依赖（许可）  
- 跨公网 / NAT 打洞需第二台机器（见 [docs/mesh-dual-machine.md](./docs/mesh-dual-machine.md)）  
- 移动端为外观预览，编排在桌面端  

---

## 10. 参与贡献与安全

- 贡献：[CONTRIBUTING.md](./CONTRIBUTING.md)  
- 安全：[.github/SECURITY.md](./.github/SECURITY.md)  
- 发布说明：[docs/RELEASE-NOTES-0.1.0.md](./docs/RELEASE-NOTES-0.1.0.md)  

---

## 11. 法律

按 [LICENSE](./LICENSE) 提供，**现状**、无担保。作者 **Pondsi** 完全免责；衍生作品须保留 Pondsi 署名。  
AI 模型输出由其提供方负责，不代表本软件立场。

赞助：[SPONSORS.md](./SPONSORS.md)

---

Pondsi (+mimo-X-pro-Preview +mimo-v2.5-pro +DeepSeek-V4.1-Flash +Qwen3.7-max +Qwen3.8-27b +Gemini3.1pro +Gemini3.8-flash) — automatically committed by Xiaomi MiMo Desktop
