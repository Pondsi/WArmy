# ADR 004：容器运行环境 —— 需求记录与设计

> 状态：**语义已定稿并落地**（P1 / P2 / P3 / P4 的**应用侧**已完成；容器内真实执行仍未接入 —— 见 §七）。
> 关联：ADR 000（不变量 #4 零原生模块、#8 紧急度归用户、#10 远程 AI 无痕）、ADR 003（组网/身份/协作）。
> 本文分四部分：**需求原文要点** → **语义定义** → **实现设计（探测 / UI / 引导 / 许可）** → **§七 定稿语义与落地事实**。
>
> ⚠️ **§七 / §八 是本 ADR 的最终口径**，与前面各节冲突时以它们为准（前面各节保留为决策过程记录）。
> §八 覆盖第七~十批的产品主更正（含**作废**的选项、不假定 Linux、快照分层、环境固化、假成功修复）。

---

## 一、需求原文要点（产品主原话，不改写）

1. **三档都要实现：**
   - **0 档**：「我的牛马」里的聊天**不需要在容器中开发**，只要有选项可以选择**是否项目的运行和测试在容器中**（选择时会提示"需要安装容器"、**好处**、**大概需要的时间**）。
   - **1 档**：**创建项目时就必须选择**该项目**是否在容器中开发**。如果选了"在容器中开发"，则**每次项目启动就必须开启容器**。
     - 这里"项目"= **这个聊天会话 + 启动/停止功能**；
     - **只有创建者可以控制**启动/停止；
     - **停止后其他成员也不能使用这个项目**，效果**等同于创建者下线**。
   - **2 档**：如果创建项目时选了**不在容器中开发**，那就和「我的牛马」一样——只有"**项目的运行和测试是否在容器中**"这个选项（同样带安装提示、好处、时间）。
2. **容器按需安装**：容器功能在用户需要时再装；**容器没启动时，控制台按钮是灰色不可使用**。
3. **探测已有容器**：探测用户环境中已有的容器，**支持常见的 10 个**（国内外常用），至少包含 **Podman、Docker、WSL** 等；"支持这些容器的探测应该不需要太多成本"。
4. **设置里新增「容器探测」**：在**设置 → 功能**中加入「**查看本机已有容器**」；探测后**列出本机所有已经有的、可以使用的容器**。
5. **未安装时的引导流**：用户在创建项目或运行项目时选了"在容器中运行"，但**实际没装容器** →
   - 出现**提示要求先安装容器**；
   - **点击后跳转到 设置 → 功能 → 容器**；
   - 那里**指引用户先点「查看本机已有容器」**，然后**列出所有本机已有的容器**；
   - **这里列出的容器可供「项目」和「我的牛马」使用**。
6. **安装说明（折叠区）**：在同一处下方列出**常用容器的安装说明**，做成**可折叠**内容：
   - **折叠时只显示"安装什么容器"**的标题/说明；
   - **展开后显示**：该容器的**简要说明**（**是否收费、是否可以商用、支持哪些系统、安装大小**等信息）、**官网地址**、**官网安装说明**、**官网下载地址**、**官网支持链接**。
7. **控制台范围**（沿用上一轮已确认）：控制台**只出现在「项目」和「我的牛马」两处的聊天里**；**联系人与群聊没有控制台**（这两处用不到容器）。

---

## 二、语义定义（把需求变成可实现的规则）

### 2.1 三个概念必须分开

| 概念 | 含义 | 谁决定 | 默认 |
| :-- | :-- | :-- | :-- |
| **开发环境**（dev） | 代码/文件编辑在哪里发生 | **创建项目时必选**，之后是否可改由产品定（建议：创建者可改，但要明确提示影响） | 项目：必选；牛马：恒为"本机" |
| **运行/测试环境**（run） | 项目的运行与测试在哪里执行 | 项目与牛马都可有**选项** | 本机 |
| **控制台** | 交互式 shell | 跟随"运行环境"所处的位置；**容器未就绪则置灰** | 不可用 |

> 「我的牛马」**没有"开发环境"选项**（恒为本机），只有"运行/测试是否在容器里"。

### 2.2 项目启动 / 停止（1 档特有）——"等同创建者下线"

> **本节已被 §七.1 定稿**（语义不变，但补上了"停止项目到底做什么"和"开发只能在容器里"两条硬结论）；
> 实现落在 `container-probe.ts` 的 `deriveContainerProjectState` + 主进程的
> `warmy:project-start` / `warmy:project-stop`。

- 项目 = **该聊天会话 + 启动/停止控制**；
- **只有创建者**能启动/停止；
- 若该项目选了"在容器中开发"，**启动项目必须先启动容器**（或启动项目时一并启动容器）；
- **停止项目 ⇒ 对其他成员的效果等同于创建者下线**：即复用 ADR 003 §2.4 / R6 的既有语义（项目主离线 ⇒ 协作面离线，本地功能仍可用），**不新增一套"停止"语义**；
- 因此：**停止 = 把该项目的协作面按"创建者下线"处理 + 把容器停掉**；其他成员看到的状态与"主离线"一致（既有 UI 已在做这件事）。

### 2.3 未就绪时的行为（不许假装）

- 选了"在容器中运行"，但**没装** → **提示 + 跳设置引导**，**不允许静默降级到本机执行**（那会让用户以为在沙箱里跑，实际在主机跑——这是最危险的误导）；
- 装了但**引擎/守护进程没起** → 同样是"未就绪"，提示"需要启动容器"；
- **容器未就绪 ⇒ 控制台按钮置灰**，并给出"为什么不可用 + 怎么装/怎么启动"的引导（走 i18n）。

---

## 三、实现设计

### 3.1 探测（核心，成本要低）

**抽象成"执行环境探测器"**：每个候选运行时 = 一段探测逻辑 + 一个"能否执行命令/开 shell"的能力声明。
探测三件事：**①命令在不在 ②版本能否取到 ③引擎是否真的可用（不只是装了个 CLI）**。

> ⚠️ 关键教训：**"命令存在" ≠ "可用"**。本机实测：`docker` 命令在（`C:\Program Files\Docker\Docker\resources\bin\docker.exe`），但守护进程没起（`failed to connect to the docker API at npipe:////./pipe/dockerDesktopLinuxEngine`）。所以探测必须区分 `not-installed` / `installed-but-not-running` / `ready` 三态。

**候选清单（国内外共 10+，按探测成本与真实可用性分组）**

| # | 运行时 | 探测方式（要点） | 备注 |
| --: | :-- | :-- | :-- |
| 1 | **Docker**（Engine / Desktop） | `docker --version` + `docker info`（后者失败=守护进程没起） | Win 上实际依赖 Docker Desktop；**见 §3.4 许可** |
| 2 | **Podman**（+ Podman Desktop） | `podman --version` + `podman info` | Win 上靠 podman machine（WSL2/Hyper-V） |
| 3 | **WSL**（Windows Subsystem for Linux） | `wsl.exe -l -v`（有无发行版）+ `wsl.exe -d <distro> -- true` | 本机实测：命令在、但**没有已安装发行版** |
| 4 | **containerd + nerdctl** | `nerdctl --version` + `nerdctl info` | CNCF，开源 |
| 5 | **Rancher Desktop** | `rdctl version` / `nerdctl` 在 PATH | 内含 k3s |
| 6 | **Colima** | `colima status` | macOS/Linux（Windows 不适用，探测直接跳过并说明） |
| 7 | **Lima** | `limactl list` | 同上 |
| 8 | **Windows Sandbox** | 检查 Windows 功能 + 版本（**家庭版没有**） | 一次性沙箱，不能当常驻运行环境 |
| 9 | **LXD / Incus** | `incus version` / `lxc version` | Linux 系统容器 |
| 10 | **iSulad**（openEuler / 华为） | `isula version` + `isulad` 服务状态 | **国内开源** |
| 11 | **PouchContainer**（阿里） | `pouch version` + `pouch info` | **国内开源** |
| 12 | **Kata Containers**（安全容器） | `kata-runtime --version` | 通常配合 Docker/containerd 使用，作为"隔离级别"选项而非独立引擎 |

**探测结果结构（结构化，不拼句子）**：
```
{
  id: 'docker' | 'podman' | 'wsl' | ...,
  displayName,                       // 由 i18n 渲染
  status: 'not-installed' | 'installed-not-running' | 'ready' | 'unsupported-platform',
  version?, engine?: { kind: 'container' | 'linux-vm' | 'system-container', api? },
  capability: { runCommand: bool, interactiveShell: bool, mountHostDir: bool },
  detail?,                          // 原始错误/证据，便于排查（不翻译）
}
```
> 注意：**WSL / Lima / Colima 严格说不是容器**（是 Linux VM / VM 管理），Windows Sandbox 是一次性 VM。探测可以统一，但**能力声明必须诚实区分**，UI 上也要说清"这是容器引擎"还是"这是 Linux 环境"——否则用户会以为拿到了容器级隔离。

### 3.2 设置 UI：功能 → 容器

- 区块标题：**容器**（i18n）；
- 主操作：**「查看本机已有容器」**按钮 → 跑探测 → **列出本机所有已有且可用的容器**（含三态与不可用原因）；
- 列表里的条目**可供「项目」与「我的牛马」选择使用**（即：探测结果是选择运行时的数据源）；
- 下方：**「常用容器安装说明」折叠区**
  - **折叠时**：只显示"安装什么容器"（例：`Docker` / `Podman` / `WSL2` / …）；
  - **展开后**：每个容器显示
    - 简要说明：**是否收费 / 是否可商用 / 支持哪些系统 / 安装大小**
    - **官网地址**
    - **官网安装说明**
    - **官网下载地址**
    - **官网支持链接**
  - 全部走 i18n（zh-CN / en-US 键必须对齐，英文包不许出现中文）

### 3.3 未安装时的引导流（跨页联动）

```
用户在[创建项目 / 运行项目]选了"在容器中运行"
        ↓
检测到没有 ready 的容器
        ↓
弹出提示：「需要先安装容器」+ [去安装] 按钮
        ↓  点击
跳到 设置 → 功能 → 容器（自动定位到该卡片）
        ↓
指引文案：先点「查看本机已有容器」
        ↓
列出本机已有容器（可用项可直接选给 项目 / 我的牛马 用）
        ↓
下方折叠区给出常用容器的安装说明（含收费/商用/系统/体积/官网四链）
```

### 3.4 许可与体积（**已联网核实的硬事实，必须如实展示给用户**）

| 运行时 | 是否开源 | 商用 | 备注 |
| :-- | :-- | :-- | :-- |
| **Docker Engine**（引擎/CLI，含 containerd/runc） | ✅ 开源（Apache-2.0） | ✅ | 不受下述订阅约束 |
| **Docker Desktop**（Win/macOS 上真正提供那个 VM 的 GUI） | ❌ **不是开源** | ⚠️ **有付费门槛**：个人/小企业/教育/非商业免费；**较大组织的商业使用需付费订阅（约 $5/用户/月起）** | **不要把它设成产品必需依赖**，否则把用户拖进许可争议 |
| **Podman / Podman Desktop** | ✅ **Apache-2.0 全开源** | ✅ | Windows 上可替代 Docker Desktop；**建议优先支持** |
| **Windows Sandbox** | 系统功能 | — | ⚠️ **Windows 11 家庭版没有**（仅 Pro/Enterprise/Education） |
| **WSL2** | 内核开源，host 侧组件为微软专有 | — | 属系统功能，**不可嵌入** |

**结论**：真正把运行时"嵌进我们的应用"不现实（VM + 虚拟化 + 几百 MB + 安装要提权）。
现实做法是 **探测并驱动用户已有的运行时**（这也与"按需安装"一致）。**优先 Podman、Docker 作为备选**（许可干净）。
**镜像来源**需产品定：联网拉公开基础镜像（如 `node:24-slim`，需要网络）vs 我们出自己的离线镜像 tar（安装包涨 100–300MB）。

### 3.5 控制台 = **容器内的控制台**（定稿见 §七.2；本节的"已暂停"已解除）

- 只在「项目」与「我的牛马」的聊天里有；联系人/群聊没有；
- **容器未就绪 ⇒ 置灰不可用**，并给引导；
- 安全边界与信任模型**由容器提供**（这是把控制台放进容器的初衷）；
- ⚠️ 此前一版把它做成了"应用内部事件日志"，那是做错了，已撤销。**事件日志不是控制台。**
  撤销后的处置：事件日志**降级为独立的诊断/排障视图**（`#console-pane` / `#btn-console`，
  文案明说"这不是控制台"），**控制台**改由 `#ctg-shell-pane` / `#btn-container-shell` 承担，
  且必须**就是容器内的 shell**（详见 §七.2 的安全契约）。

---

## 四、分期建议（避免一次吞太多）

| 期 | 内容 | 依赖 |
| :-- | :-- | :-- |
| **P1** | 探测器（10+ 运行时、三态）+ 设置里「容器」卡片（查看已有 + 列出）+ 折叠安装说明（含许可/体积/四链）+ 未就绪引导流 | 无（纯本机命令探测，零依赖） |
| **P2** | 「我的牛马」与「项目」的"运行/测试在容器中"选项 + 未就绪时的硬提示（**不静默降级**） | P1 |
| **P3** | 项目创建时必选"是否在容器中开发" + 启动/停止（仅创建者）+ "等同创建者下线"语义 | P2 + ADR 003 既有主离线语义 |
| **P4** | 控制台（容器内 shell；未就绪置灰）+ 真实执行路径（工具调用/命令在容器内跑） | P1–P3 + 安全评审 |

## 五、明确不做 / 待定

- **不做**：把容器运行时打进我们的安装包（不现实）；把 Docker Desktop 设为必需依赖（许可风险）。
- **待产品定**：镜像来源（联网拉 vs 离线镜像）；开发环境选定后能否中途切换；"容器内开发"时项目目录怎么挂载（bind mount 在 Windows 上小文件性能差，需实测）；`wsl --install` 之类需要提权的安装步骤是否由我们代跑（**建议：只给指引，不代跑**）。

---

## 六、语义澄清（产品主第七轮确认，**以此为准**）

### 6.1 「在容器中开发」= **bind mount 整个项目文件夹**

产品主明确：**用 bind mount 挂载宿主项目目录**，而且——**把该项目的整个文件夹都放进去，该项目（聊天）本身也全部放进去**。

即：容器看到的不是"某个源码子目录"，而是**这个项目（含这个聊天）的完整目录**。带来的必然结论：

- 容器里对项目文件的改动**就是宿主上同一份文件的改动**（同一份 inode 经挂载可见），**不存在"把产物拷出来"这一步**；
- 宿主上的编辑器 / 预览 / 打包看到的就是容器里刚改完的东西，**这就是"实时"的实现方式**；
- ⚠️ **代价要如实告知**：Windows 上这条挂载走 WSL2，**大量小文件操作明显慢**（git 仓库尤其痛）。这是取舍，不是能靠实现绕开的 bug。

### 6.2 容器 = **开发环境（安全隔离）**，原则上**不用于项目测试**

产品主明确：**用户选择容器时，UI 要说明这是"开发环境"、用途是"安全隔离"，原则上不用于项目测试。**

由此修正前面几轮的表述：

| 项 | 修正后的语义 |
| :-- | :-- |
| 容器的定位 | **开发环境**；目的是**安全隔离**（把 AI 生成的命令/工具调用关进边界里，而不是放主机上跑） |
| 是否用于测试 | **原则上不用于项目测试**。UI 文案不许暗示"选了容器就能跑测试拿结论" |
| UI 文案要求 | 选项/说明里必须写明：**这是开发环境，用于安全隔离，原则上不用于项目测试** |
| 与"运行/预览目标"的关系 | §三 里"构建/测试环境 vs 运行/预览目标"的二分，**改为**：容器 = 开发（隔离）；**运行/预览仍在目标平台**（Windows 桌面→Windows 宿主/VM；macOS→Mac；Web→容器 dev server + 端口映射到宿主浏览器；Android→宿主模拟器/真机；iOS→Mac） |
| 与"测试环境"的关系 | 若将来确实要做"容器内跑测试"，必须**单独**提出并说明与"容器=开发环境"的区别，不许默默把开发环境当测试环境用 |

### 6.3 由此产生的两条硬要求

1. **绝不允许静默降级**：选了"容器中开发"但没有就绪的容器，必须**明确告知并引导安装/启动**，**不许退到主机上悄悄跑**（用户会以为在隔离环境里，实际在主机上——这是这类功能里最危险的误导）。
2. **跨平台预期必须先讲清**：容器是 Linux 的，**给不了 Windows / macOS 的图形界面**。UI 必须在用户选容器时就说清"**界面在哪个平台预览**"，否则用户会等一个永远无法满足的效果，并把它当成 bug。

---

## 七、定稿语义与落地事实（产品主第七轮确认，**与前面各节冲突时以本节为准**）

### 7.1 容器项目 = **只能在容器里开发**（这是本轮新增的最硬的一条）

| 项 | 定稿结论 |
| :-- | :-- |
| 开发/编辑发生在哪 | 项目创建时选了「开发环境 = 容器」⇒ **该项目的开发与编辑只允许发生在容器里**。项目目录在宿主上确实看得见（bind mount，§6.1），但**让本应用在宿主侧编辑它就是把整个安全承诺作废** —— 所以本应用**不提供**宿主侧编辑这条路（不是"不推荐"，是**不给**） |
| 容器没运行时 | **该项目等于「已停止」**：成员也不能编辑/开发它。对外的可见效果**与"创建者下线"完全一样** —— 直接复用 ADR 003 §2.4 / R6 的既有语义，**不新造第三种状态** |
| 「停止项目」的含义 | 由此确定：**停止项目 = 确保容器停下来 = 项目不可开发**（效果等同创建者下线）。**只有创建者**能启动/停止。它终于有了确切定义 —— 上一轮正是因为这条语义未定才没有实现 |
| 测试 / 运行 | **只有"测试 / 运行"可以留在宿主或其它设备上**；开发不行（与 §6.2「容器 = 开发环境」一致） |
| 例外与模糊地带 | 无例外：只要选了"容器中开发"，**宿主侧编辑一律被拒**（这条承诺没有"就这一次"） |

**判定只有一份实现**（任何地方都不许再猜一套）：
`packages/app-shell/src/container-probe.ts` → `deriveContainerProjectState()`（纯函数）。
它输入四个事实（开发环境选择 / 所选运行时 / **运行时最近一次真探测的状态** / 创建者是否点过停止），
输出结构化状态：

```
{ devEnv, containerOnly, running, stopped, code,
  hostEditingRefused, developmentAllowed, developmentWhere,     // 开发：唯一允许的地点是容器
  testingAllowed: true, testingWhere: 'host-or-other-device',  // 测试/运行：可留在宿主或其它设备
  memberFace: 'creator-offline' | null,                        // 成员侧 = 既有「离线」那一套
  restrictions }
```

状态码（机器可读，文案走 i18n）：`host-dev` / `ok` / `stopped-by-creator` /
`container-not-ready` / `container-not-installed` / `container-not-chosen`。

**"拿不准"一律当停止**：还没探过（`runtimeStatus = null`）、探测失败、没选定用哪个容器 ——
全都算停止态。宁可不给开发入口，也**绝不**乐观放开宿主侧编辑。

#### 应用侧怎么执行的（已落地）

1. **主进程拒绝**：容器项目的开发面在容器没就绪时，`warmy:chat-send` 与
   `warmy:group-orchestrate` 直接回结构化拒绝
   （`code: 'project-stopped'` / `reasonKey` / `memberFaceKey: 'group.memberOffline'` /
   `hostExecutionRefused: true`），**不静默退到主机上跑**。
2. **渲染层不提供宿主侧编辑**：停止态下输入框与发送按钮**禁用**（`data-dev-blocked="1"`），
   并给出原因；右栏项目卡片显示"已停止 + 原因 + 等同创建者下线 + 诚实边界"。
3. **成员侧与"创建者下线"同一套**：同一个标志位（`data-member-face="creator-offline"`）
   + 同一句既有文案（`group.memberOffline`），**不是**新造的状态或措辞。
4. **「停止项目」真的走完语义**：落盘 `settings.containerStopped[groupId]`，并对**可程序化停止**的
   运行时请求停引擎（`container-action` 那条带二次确认的既有通道）；对不能程序化停止的
   （WSL / 系统服务 / 一次性沙箱）**如实说明**，不假装已经停了。
   同时创建者声明停止后，即使引擎还开着，项目也**是**停止态（"停止 = 不可开发"）。

#### ⚠️ 诚实的强制边界（必须如实写在报告与 UI 里）

**我们能做到的**：
- 容器没起来时**拒绝写 / 改项目文件**（主进程层面拒绝这一轮开发）；
- 把项目显示成停止态、并**禁用开发入口**；
- 向成员如实说明状态与原因。

**我们做不到的**：
- **阻止用户自己用外部编辑器打开那个目录**（bind mount 让宿主看得见它，这是设计的一部分）。

所以这是**应用侧强约束 + 明确告知**，**不是**文件系统级强制。
要不要上文件系统级措施（目录 ACL / 只读挂载 / 改成容器内独立副本再同步回宿主等）**必须另行拍板**，
本轮**不实现**；而且"只读挂载"与 §6.1 的"实时编辑"直接冲突，属于产品取舍，不是实现细节。

### 7.2 控制台 = **容器内的控制台**（因为它的价值就是给主机带来安全性防护）

- **是什么**：运行中的容器里的 shell（`#ctg-shell-pane`，入口 `#btn-container-shell`）。
  它不是"应用内部事件日志"——那一版做错了，已撤销。
- **在哪**：只在「项目」与「我的牛马」的聊天里；联系人与群聊**没有**（不是灰着占位）。
- **什么时候可用**：门禁顺序固定，任一条不满足就**一条命令都不执行**：
  ① 不在这两处聊天 → 不显示；② 会话没开"运行/测试在容器中" → 置灰（`notEnabled`）；
  ③ 容器项目已停止 → 置灰（`projectStopped`，等同创建者下线）；
  ④ 所选运行时不是 `ready`（含"没探过"）→ 置灰（`notReady`）+ 走 §3.3 引导流；
  ⑤ 引擎就绪但**项目容器镜像还没定** → 面板**可以打开**（好把"还差什么"说清），
  但**输入行禁用**、一条命令都不跑（今天本机就处在这一档）。
- **安全契约**（写进代码常量 `CONTAINER_SHELL_SECURITY`，随每次响应回传，可断言）：
  - 只有**本机的人手动输入**才会执行（`typedBy: 'local-human-only'`）；
  - 远程对端 / 群成员 / 任何智能体 / 任何网络内容**没有注入路径**（`remoteInjectPaths: 0`）；
  - **不自动执行**任何命令（`autoRun: false`）；
  - 本机密钥类环境变量（API Key / SMTP 授权码 / 身份私钥）**不**带进容器（`forwardsSecretEnv: false`）；
  - IPC 参数形状只有 `{ runtimeId, action }`，**action 是枚举**（`open|write|close|status`），
    **不接受任何命令字符串**；`data` 只作为"已打开的本地 shell 的 stdin"，有长度上限，
    **绝不**被拼进任何命令行（`argvFromUntrustedSource: false`）；
  - 容器没就绪时**不会**退化成宿主执行，也**不会**回退成事件日志（`hostFallback: false`）。
- **事件日志的处置**：降级为**独立的诊断/排障视图**（`#console-pane` / `#btn-console`），
  `console.hint` / `tip.console` 都已改口，明说"这是诊断事件流、**不是控制台**"。
  选择"降级"而不是"删除"的理由：它对排障仍有真实价值（工具调用、组网事件、错误、凭据打码、
  行数上限、按 seq 去重），而且**把两者名字与位置彻底分开**之后，混为一谈的风险已经消除。

### 7.3 本轮落地 / 未落地（不许含糊）

| 项 | 状态 |
| :-- | :-- |
| 探测器（12 个候选、三态、诚实能力声明、启停参数校验） | ✅ 已落地（`verify-container-probe.mjs` 真机断言） |
| 设置 → 功能 → 容器（查看已有 / 折叠安装说明 / 许可与体积 / 引导流） | ✅ 已落地 |
| 「我的牛马」与「项目」的运行/测试环境选项 + 未就绪硬提示（不静默降级） | ✅ 已落地 |
| 项目创建必选开发环境 + 启动/停止（仅创建者）+ **停止 = 等同创建者下线** | ✅ 已落地（本轮） |
| **容器项目 = 只能在容器里开发** + 宿主侧编辑被拒 + 成员侧等同离线 | ✅ 已落地（本轮，见 §7.1） |
| 控制台 = 容器内的 shell + 门禁 + 安全契约 + 未就绪不执行 | ✅ 应用侧已落地（本轮）；**真实容器内执行仍未接入** |
| 容器内**真实**执行（拉镜像 / 起容器 / 真 shell / 工具调用跑在容器里） | ❌ **未落地**：需要项目容器镜像，而镜像来源（联网拉公开镜像 vs 我们出自己的离线镜像）**仍待产品主决定**；在决定之前**不拉镜像、不起容器**。因此"真 shell 能跑命令"这条**本轮无法验证**，如实标为未验证 |
| 文件系统级强制（阻止外部编辑器直接改项目目录） | ❌ **未实现**，且必须先拍板（见 §7.1 的边界说明） |

### 7.4 验收口径（这一轮实际怎么验的）

- **主进程/纯逻辑**：`packages/app-shell/scripts/verify-container-probe.mjs`（真机探测 + 参数白名单 +
  门禁五档 + 项目状态推导 19 条 + "停止态必须能对上既有离线文案"）。
- **渲染层/真浏览器**：`packages/app-shell/scripts/verify-net-ui.mjs` 第 18 节（**本节已按定稿重写**：
  门禁四档、面板到底是什么、IPC 参数形状与"未就绪不执行"、容器项目停止态的 UI 面与
  「停止项目」的完整流程（两次确认 + 真的发出停引擎请求）、诊断事件流降级后仍然如实）。
- **静态接线**：第 18 节里对源码的断言（容器内执行路径**今天不 spawn 任何进程**、参数白名单共享、
  旧说法已从语言包删除等）。
- **未验证（如实标注）**：容器内**真实**执行路径（要镜像）、真实引擎启停（要真机环境，
  另有 `verify-container-real.mjs` 属于"会改本机状态、刻意不进默认门禁"的那一类）、
  文件系统级强制（未实现）。

---

## 八、第七~十批：定稿补充与更正（**与前面冲突时以本节为准**）

### 8.1 ❌ 「运行/测试在容器中」整块**作废删除**（第七批）

- 产品主定稿：**容器 = 开发环境**；**测试与运行不在容器的职责内** ⇒ 该选项连同它的
  存储（`containerRun`）、右侧顶部的容器下拉框、「…」菜单里的勾选项、首次运行询问、
  以及那一整套文案**全部删除**（不是藏起来）。
- 落地事实：`settings-store` 只留一条"已作废"的只读说明；渲染层删掉
  `renderRunEnv / toggleRunEnvFromMenu / maybeAskContainerFirstRun / containerRecord …`；
  i18n 删掉 `container.runEnv.hard*/menu.*/current.*/firstRun.*/switch.*/pick.*`（仍被设置页
  引用的"环境类型事实"键保留）。
- **开发环境只在创建项目时选一次**（本机 / 容器中），之后不再提供"随时开关"。

### 8.2 「不可用 = 只能翻看记录」与「启用/停用项目」（第七批）

- 容器开发项目 **容器没启动 ⇒ 项目不可用**：置灰、不可聊天、其中功能不可用，
  **但历史记录仍然可读**（`historyReadable: true` 是**恒真的事实**，不是可选开关）。
- 对成员的可见效果**等同「创建者下线」**（同一标志位 + 同一句既有文案 `group.memberOffline`）。
- **不误伤**：创建时没选容器的项目、以及「我的牛马」的聊天，**无需容器也能正常聊天**。
- **启用/停用项目**：入口在**项目右键菜单**（与是否选容器无关）；
  **容器没启动时不能启用**，提示"需先到设置中启动容器"并给与之前一致的跳转引导。
  只有创建者能启用/停用（判定沿用 group-store 的 `creatorFingerprint`）。
- **切换容器**：只属于"创建时选了容器开发的项目"，入口同样是**项目右键菜单** →
  **独立弹窗**（列出设置里已检测到的容器 + 「添加更多容器 → 跳转设置」）；
  **项目正在运行时切换 ⇒ 提示"重启项目才能生效"**。右侧顶部**不再**有切换入口。

### 8.3 右侧三块新内容（第七批）：真实数据 or 如实空态

| 面板 | 真实来源 | 拿不到时 |
| :-- | :-- | :-- |
| **最近改动文件** | 检查点明细（`checkpoints.list()` 的 `filesChanged/filesCreated` + 真实 mtime） | 如实空态 + 说明"本机还没有项目目录记录" |
| **其他文件（非项目内）** | 同上，取不在项目目录下的路径 | 如实空态 + **写明还缺哪类数据源** |
| **生成的产品** | 产物目录约定 `<userData>/products/<sessionId>` + 入口识别 | 显示**即将存放这个生成产品的目录**（标明"尚未创建"），不是空白也不是"无" |

- **产物运行按钮**：产物是**程序**时显示入口文件 + 「运行」；**只有"运行环境与主机一致"
  （本机开发 + 宿主原生入口）才可点**，其余一律置灰并说明原因（容器里产出的产物在主机上不可运行）；
  点击后**真的在主机上运行**（入口路径**由主进程自己解析**，渲染层只送 `sessionId`）。
- **数据源缺口（如实报告，不填假数据）**：
  - `tool-file-access-ledger`：工具调用（read/write/delete/create）的**路径台账今天不存在**
    （`helper-tool` 只写文件、不记路径；`checkpoint.ts` 不在本轮可改范围）；
  - `project-directory-record`：项目目录**没有被记录**（`GroupRecord` 没有目录字段）。
  这两个缺口已写进 IPC 响应（`missingSources`）并在面板上如实显示。

### 8.4 部署环境由用户自装 + 内置「安装提示词」（第八批）

- **不预装** Node/Python 等：镜像更小、首启更快；我们只给**指引、建议与安装方法链接**，不代跑。
- **持久性**（必须讲清）：在容器**可写层**里装的东西，停止/启动同一容器会保留；
  **删掉重建就没了** ⇒ 给出三种做法（别重建 / 写下步骤 / 用「固化当前环境」）。
- **网络**：容器内装包要联网，国内可能要换镜像源（apt/pip/npm）—— 给出指引，不代配。
- **安装提示词**：在容器设置卡片里放一段**可一键复制**的提示词（中英各一份，走 i18n），
  内容必须含：挂载点 `/workspace`（并说明以真实挂载为准）、**先探测再动手**
  （`cat /etc/os-release` / `uname -m` / `which node python3 java go`）、按真实发行版选包管理器、
  说明"装了什么/装在哪/怎么验证"并写下步骤、提醒重建会丢与固化建议、
  以及 ⛔ **不要把密钥/token/密码写进容器文件**（commit 会把它们固化进镜像）。

### 8.5 快照与回退点是**分层**的（第八批）

- **bind mount 下容器快照覆盖不到项目文件**：`docker commit` 快照的是**容器可写层**，
  项目目录是宿主挂载进来的、在快照之外 ⇒ **"有了容器，项目回退就更好做"不成立**。
- 分层：
  - **文件回退 = 现有回退点机制**（git / shadow-git / CoW）：**不依赖容器**；
    **没有容器也必须有回退点**。
  - **环境回退 = 容器镜像 / 快照**：装好依赖后固化一下，下次直接起 —— 这才是快照擅长的。
- **增益（已实现）**：回退点升级为 **"文件 + 环境指纹"** —— 每个回退点顺便记录当时的
  **运行时 + 镜像 digest 快照（revision）**；回退后**如实对比**：环境变了就提前告知
  "文件回退了、环境回不去，可能需要重装依赖"。
- **不许撒谎**：不能对用户声称"有容器回退点就更简单了"。

### 8.6 不假定 Linux + 环境固化（第九批）

- **不得写死"Linux 容器"**：数据结构携带 `runtimeId` 与**引擎的系统模式**
  （Docker 的 Linux / Windows 容器模式、WSL 的发行版），**从真探测读**（读不到就 `unknown`）；
  文案说"你选择的容器 / 运行环境"。**已实现**：`engineOsModeOf()`（纯函数，12 个运行时全覆盖断言）。
- **能力按运行时区分**（已实现 `envSolidifyCapability()`）：

  | 运行时 | 固化能力 | 说明 |
  | :-- | :-- | :-- |
  | Docker / Podman / nerdctl / Rancher | `commit`（可程序化） | 把当前状态固化成一层 |
  | **WSL** | `export-import`（**不可程序化**） | **WSL 没有 commit**：只能整盘 `wsl --export/--import`（很大很慢），我们**不代跑** |
  | Windows Sandbox | `unsupported` | 一次性 VM，无常驻环境 |
  | LXD/Incus | `commit`（需提权，不代跑） | 系统容器由系统服务托管 |
  | Kata | `unsupported` | 只是隔离级别，不是独立引擎 |

- **固化策略（已实现为纯函数，可断言）**：
  1. **默认不删项目容器**（长期存在、不用 `--rm`）⇒ 停止/启动保留可写层；
  2. **在明确的时间点固化**（**不是"任何文件变动就固化"**）：
     ⚠️ `docker diff` 只能看到可写层变了几文件，**分不清"装了个包"和"缓存/日志/临时文件"** ⇒
     **节流**（默认 45s 内只固化一次）+ 时机（显式按钮 / **可能销毁容器之前一定固化一次**）；
  3. **保留策略**：固化产物只留最近 **3** 个，可手动清理；
  4. **记住每个项目用哪个容器**：`runtimeId` + 容器引用 + 上次固化镜像 + 环境指纹；
  5. **安全提醒**：commit 会把**当时的整个文件系统**一起固化（**包括可能的密钥/缓存/临时文件**），
     所以固化入口旁必须写清，且我们**不**自作主张删用户文件。
- ⚠️ **真实 commit 仍未执行**（与容器内 shell 同一条纪律）：镜像来源未定之前不拉镜像、不起容器，
  因此 `warmy:project-env-solidify` 只会**如实拒绝**并说明该怎么做，**绝不写假的"已固化"记录**。

### 8.7 修一个真产品 bug：启停"假成功"（第十批）

- **现象**（真机实测）：点「启动容器」返回 `{ok:true, accepted:true}`，但引擎**根本没起来**
  （CLI 立刻 rc=1），真实失败只留在探测报告的 `action.result` 里，用户看到"已启动"却永远未就绪。
- **修复**（已实现）：
  1. 「进程成功派生」**不再等于成功**：动作结果带 **`reasonCode`**，
     由 `classifyActionResult()` 从**原始输出**归类（不认识就如实给一般失败码，不编原因）；
  2. 新增原因码与 i18n（中英对齐）：`install-incomplete`（**引擎安装不完整或未能启动**）、
     `engine-start-failed`、`engine-stop-failed`、`no-exit-code`、`spawn-failed`；
  3. UI 把**原因文案 + 原始输出行**一起摆出来（次要样式，对比度 ≥ 3.0）；
  4. **失败后按钮恢复可用**、过渡态清掉（不会卡在"正在启动…"），并留重试入口。
- 用注入桩验收："派生了但 rc=1 且从未就绪" ⇒ **绝不显示成功**、显示原因与原始输出、按钮恢复。

### 8.8 两条从真机线得到的通用结论（写在这里避免以后再踩）

1. **"发行版存在" ≠ "发行版可用"**：`wsl -l -v` 报 Ubuntu 为 Running，但
   `wsl -d Ubuntu -- <cmd>` **吊死 26 分钟无输出**（首启 OOBE 未完成）⇒
   探测**必须真的执行一条命令**才算就绪（现有 `distro-start-timeout` 信号是对的）。
2. **能力声明必须按运行时区分**（见 §8.6 表）：不能给一个"点了必然失败"的按钮。
