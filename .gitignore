node_modules/
package-lock.json
dist/
release/
*.log
.env
.env.*
*.db
*.db-journal
*.db-shm
*.db-wal
.DS_Store
Thumbs.db
*.tsbuildinfo

spikes/node_modules/
spikes/package-lock.json
spikes/*/node_modules/
spikes/*/models/
spikes/*/dsh-memory-plus/


spikes/spike-07-onnx/models/
spikes/spike-05-plugins/dsh-memory-plus/
spikes/spike-05-plugins/node_modules/
spikes/node_modules/


vendor/agency-agents/

# spike-08 的 hosts 备份是证据，但含本机 hosts 内容，不入库
spikes/spike-08-helper/backups/

# build-preview.mjs 的默认输出目录（构建产物，验证器会把产物输出到交付目录，不入库）
packages/app-shell/preview/

# 捆绑 Node 运行时（按入库清单有意不跟踪：单个 node.exe 就 89MB，由安装脚本按平台解压）。
# 加进 .gitignore 是为了防止有人 `git add -A` 时把它误提交进仓库。
resources/node/

# electron-builder unpack output at repo root (build artifact)
win-unpacked/
packages/app-shell/win-unpacked/
# phone-simulator entry is intentional (product page); keep it
# but do not commit local temp profiles
**/profile-net/
**/profile-qr/
